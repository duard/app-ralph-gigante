# Operational Details for Agents

## Scripts Available

- `format`: prettier --write .
- `format:check`: prettier --check .
- `prepare`: husky

No `lint`, `typecheck`, `test`, or `build` scripts available in package.json.

Use `format:check` for code quality checks.
